{
  "name": "whatsapp-bot",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "node ./client.js --no-sandbox",
    "dev": "nodemon ./client.js --no-sandbox",
    "deploy": "pm2 start ./client.js --name whatsapp-bot -- --no-sandbox"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.19.5"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
